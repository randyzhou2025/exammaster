---
description: 仅在用户明确确认后才提交代码
alwaysApply: true
---

# 提交前等待用户确认

完成任何任务后，**不要**主动 `git add` / `git commit` / `git push`。

## 流程

1. 改完代码 → 保持工作树未 stage 状态（便于直接看 diff）。
2. **停下来**，把改动以简短列表/对比表的形式总结给用户。
3. 等用户明确说出 **"commit"** / **"push"** / **"提交"** / **"推送"** 等关键词后，再执行版本控制命令。

未得到确认前不要：

- `git add`（除了为了 `git diff --cached` 临时演示，演示完立刻 `git reset`）
- `git commit`
- `git push`
- `git revert` 已 push 的内容（除非用户先要求回滚）

## 例外

仅以下情况可不等待，但仍应在执行前一句话告知：

- 用户在同一条消息里直接说 "改完顺手 commit" / "做完 push 上去"。
- 用户在前一条消息里给出了「持续工作循环」的指令（如 babysit/loop 流程）并且明确允许期间自动提交。

## 多步任务

若任务包含多个原子步骤，每完成一步就报告，不要把多步合并连续 commit。是否合并由用户决定。
