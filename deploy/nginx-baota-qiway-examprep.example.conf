# 示例：宝塔「站点配置文件」中与 ExamMaster 相关的片段（qiway.site）
# 1. 将 deploy/welcome/ 整目录上传到服务器，例如：/www/wwwroot/qiway.site/welcome/
# 2. .env.prod 设置 VITE_BASE_PATH=/examprep/ 后重新 build 前端镜像
# 3. 删除旧的 location ^~ /api（指向 cyber 3001）整段；根路径反代改为下列写法

# proxy_cache_path ... （可保留宝塔原有）

# server { ... 内：

#     location = /examprep {
#         return 301 https://$host/examprep/;
#     }

#     location ^~ /examprep/ {
#         proxy_pass http://127.0.0.1:3010/;
#         proxy_set_header Host $http_host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 600s;
#         proxy_read_timeout 600s;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection $connection_upgrade;
#     }

#     location ^~ /api/ {
#         proxy_pass http://127.0.0.1:3010/api/;
#         proxy_set_header Host $http_host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 600s;
#         proxy_read_timeout 600s;
#         proxy_http_version 1.1;
#     }

#     location = / {
#         root /www/wwwroot/qiway.site/welcome;
#         try_files /index.html =404;
#     }

# 其余路径（如旧站 /uploads）按需保留；不要再写 proxy_pass 整站到 3010 的 location ^~ /
